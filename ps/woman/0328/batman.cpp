#include <cstdio>
#include <string>
using namespace std;

int temp;


int main() {
    int T, N;
    scanf("%d", &T);
    for (int tc = 0; tc < T; tc++) {
        scanf("%d", &N);
        for (int i = 0; i < N; i++) {
            scanf("%d", &temp);
        }
    }
    return 0;
}
