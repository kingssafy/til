#include <iostream>
using namespace std;
const int MAX = 5000;
int arr[MAX] = {};
int N;


int main() {
    scanf("%d", &N);
    int sl, sw;
    for (int i = 0; i < N; i++) {
        scanf("%d %d", &sl, &sw);
        
    }

    return 0;
}
